package tpm.yu.mol.share.util

object SaConstant {
    object Error {
        const val nullValue = "Value[%s] null";
        const val emptyString = "String[%s] null or empty"
        const val emptyList = "List/Map [%s] null or empty"
        const val invalidType = "Type[%s] invalid"
    }
}
